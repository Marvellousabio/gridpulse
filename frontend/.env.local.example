# Mock mode (default) — no env needed
# Live GridPulse /v1 API:
# NEXT_PUBLIC_API_BASE=https://your-api-host

# Optional FastAPI dashboard backend (legacy integration):
# NEXT_PUBLIC_API_URL=http://localhost:5000
