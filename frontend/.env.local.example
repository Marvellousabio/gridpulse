# Mock mode (default) — no env needed

# Live GridPulse /v1 API (backend on port 5000):
NEXT_PUBLIC_API_BASE=http://localhost:5000

# Optional legacy /api/* dashboard backend:
# NEXT_PUBLIC_API_URL=http://localhost:5000
